{
  "name": "sdk-panel-vercel",
  "version": "1.0.0",
  "description": "MUNDO + BCORE SDK Panel — Vercel compatible (TypeScript serverless + static frontend, mobile-first)",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "vercel dev"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
